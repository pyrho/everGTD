extends ../layout
block content
  script.

  for note in notes
    p
    if note.tags.length > 0
      h1= note.title + "|" + note.tags
    else
      h1= note.title
    //a(href="https://sandbox.evernote.com/Home.action#st=p&n=#{note.guid}" 
    pre= note.content
